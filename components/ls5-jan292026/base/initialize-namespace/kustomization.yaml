apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Task definition (self-contained, no external dependency)
  - dev-namespace-setup-task.yaml
  # Job that triggers the Task via PipelineRun
  - initialize-namespace.yaml

